<% provide(:title, 'Plentus - Beta Sign Up') %>
<main class="bs-secondaryhead" role="main">
	<div class="container secondary">
	<div class="row">
	<div class="col-xs-12">
		<div class="logo logo-inset"></div>
		<div class="outer-box-transparent">
			<div class="inner-box">
				<div class="note">Sign up below for Plentus</div>

<%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
<%= hidden_field_tag 'plan', params[:plan] %>
  	<form role="form">
	  <div class="form-group">
	<%= f.input :fullname, label: 'First & Last Name' %>
	</div>
	<div class="form-group">
	<%= f.input :email, label: 'Email Address' %>
	</div>
	<div class="form-group">
	<%= f.label :country_id %><br />
	<%= f.collection_select :country_id, Country.order(:name), :id, :name, include_blank: true %>
	</div>
	<div class="form-group">
	<%= f.label :state_id, "State or Province" %><br />
	<%= f.grouped_collection_select :state_id, Country.order(:name).includes(:states), :states, :name, :id, :name, include_blank: true %>
	</div>
	<div class="form-group">
	<%= f.input :password, label: 'Password' %>
	</div>
	<%= f.submit "Sign up", :class => "btn btn-submit"%>
	<% end %>
	
	</form>
</div>
</div>
</div>
</div>
</div>
</main>


	
				